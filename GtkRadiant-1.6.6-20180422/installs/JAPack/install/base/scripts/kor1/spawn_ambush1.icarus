//Generated by BehavEd

rem ( "comment" );

affect ( "player", /*@AFFECT_TYPE*/ FLUSH )
{
	rem ( "darkside = 2" );

	if ( $get( FLOAT, "SET_OBJECTIVE_LIGHTSIDE")$, $=$, $2$ )
	{
		rem ( "spawn light side enemies" );
		use ( "ambush1_light" );
	}


	else (  )
	{
		rem ( "spawn dark side enemies" );
		use ( "ambush1_dark" );
	}

}

